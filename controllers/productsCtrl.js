'use strict'

module.exports.getProducts = (req, res, next) => {
  console.log("Product");
  // const { Products } = req.app.get('models');
  // Products.findAll()
  // .then( (products) => {
  //   res.render('product', {products});
  // })
  // .catch( (err) => {
  //   next(err); 
  // });
};